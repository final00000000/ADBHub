# Git Skip-Worktree 配置指南

## 什么是 skip-worktree？

`skip-worktree` 让 Git 忽略本地文件的修改，适用于：
- 包含 AI 工具生成内容的文件
- 个人开发配置
- 本地调试信息

与 `.gitignore` 的区别：
- `.gitignore`: 完全不跟踪文件
- `skip-worktree`: 文件已提交，但忽略本地修改

## 推荐设置 skip-worktree 的文件

```bash
# 初始化 git 仓库（如果还没有）
git init

# 设置 skip-worktree（根据需要选择）
git update-index --skip-worktree local.properties
git update-index --skip-worktree gradle.properties
git update-index --skip-worktree app/build.gradle.kts

# 如果有包含 AI 辅助注释的文件
git update-index --skip-worktree path/to/file-with-ai-comments.kt
```

## 查看已设置的文件

```bash
git ls-files -v | findstr /B S
```

## 取消 skip-worktree

```bash
git update-index --no-skip-worktree 文件路径
```

## 批量设置脚本

创建 `setup-skip-worktree.ps1`:

```powershell
# 需要设置 skip-worktree 的文件列表
$files = @(
    "local.properties",
    "gradle.properties"
    # 添加其他需要的文件
)

foreach ($file in $files) {
    if (Test-Path $file) {
        git update-index --skip-worktree $file
        Write-Host "✓ 已设置 skip-worktree: $file"
    } else {
        Write-Host "✗ 文件不存在: $file"
    }
}
```

运行: `powershell -ExecutionPolicy Bypass .\setup-skip-worktree.ps1`
