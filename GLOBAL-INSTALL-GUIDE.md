# 全局 Git Hooks & Claude Skills 安装指南

## 📦 已安装内容

### 1. 全局 Git Hooks
- **位置**: `C:\Users\17692\.git-hooks\`
- **功能**: 自动阻止包含 AI 署名的提交
- **作用范围**: 所有 Git 仓库（全局生效）

### 2. Claude Code Skills
- **位置**: `C:\Users\17692\.claude\skills\`
- **已安装**:
  - `gitmoji-commit.md` - 带 emoji 的规范化 Git 提交
  - `grill-me.md` - 深度讨论和方案评审

## 🚀 快速开始

### 方式一：使用安装脚本（推荐）

```powershell
# 在项目目录运行
powershell -ExecutionPolicy Bypass .\install-global.ps1
```

### 方式二：手动验证

```powershell
# 检查全局 hooks 配置
git config --global core.hooksPath

# 应该输出: C:\Users\17692/.git-hooks

# 检查文件是否存在
ls C:\Users\17692\.git-hooks
ls C:\Users\17692\.claude\skills
```

## 📝 使用方法

### Git Hooks（自动生效）

对所有仓库自动生效，无需手动配置。

**测试:**
```bash
cd <任意-git-仓库>

# 正常提交（会成功）
git commit --allow-empty -m "test: normal commit"

# 包含 AI 署名（会被拒绝）
git commit --allow-empty -m "test: with ai

Co-authored-by: Claude <claude@anthropic.com>"
```

### gitmoji-commit Skill

在 Claude Code 中使用：

```bash
/gitmoji-commit
```

**功能：**
- 自动生成带 emoji 的 Conventional Commit 格式
- 支持中英文
- 自动检查 git 状态
- 安全的文件暂存
- 支持 PR 创建
- 禁止 AI 署名

**示例输出：**
- `✨ feat(ui): add dark mode support`
- `🐛 fix(auth): resolve token expiration issue`
- `📝 docs(readme): update installation steps`
- `♻️ refactor(api): simplify error handling`

### grill-me Skill

在 Claude Code 中使用：

```bash
/grill-me
```

**功能：**
- 深度询问设计方案的每个细节
- 逐步解决决策树的每个分支
- 为每个问题提供推荐答案
- 一次提出一个问题，直到达成共识

**适用场景：**
- 压力测试你的技术方案
- 评审设计文档
- 探讨架构决策
- 发现潜在问题

## 🔧 配置详情

### 全局 Git 配置

```bash
# 查看当前配置
git config --global --list | grep hooks

# 应该看到:
# core.hookspath=C:\Users\17692/.git-hooks
```

### Hook 检测的 AI 署名类型

- `Co-authored-by: Claude`
- `Co-authored-by: ChatGPT`
- `Co-authored-by: Copilot`
- `Generated by AI`
- `AI-assisted`
- `With assistance from`
- `Pair programmed with`
- `Claude Code`
- `Cursor AI`
- `Codeium`

## 📂 文件结构

```
C:\Users\17692\
├── .git-hooks\
│   ├── commit-msg              # Bash wrapper
│   └── commit-msg.ps1          # PowerShell hook 脚本
└── .claude\
    └── skills\
        ├── gitmoji-commit.md   # Gitmoji 提交 skill
        └── grill-me.md         # 深度讨论 skill
```

## 💡 高级使用

### 临时禁用 Hook

如果某次提交确实需要跳过检查：

```bash
git commit --no-verify -m "your message"
```

⚠️ **不推荐**：这会跳过所有 hooks，包括安全检查。

### 在特定仓库禁用全局 Hooks

```bash
cd <你的仓库>
git config core.hooksPath .git/hooks
```

这会让该仓库使用本地 hooks 而非全局 hooks。

### 查看 Skills 列表

在 Claude Code 中：

```bash
/help
```

会显示所有可用的 skills。

## 🐛 故障排除

### Hook 未生效

```powershell
# 检查全局配置
git config --global core.hooksPath

# 如果为空，重新设置
git config --global core.hooksPath "$env:USERPROFILE/.git-hooks"

# 检查文件权限
Get-Acl C:\Users\17692\.git-hooks\commit-msg.ps1
```

### PowerShell 执行策略错误

```powershell
# 临时允许
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass

# 或全局允许（谨慎）
Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned
```

### Skill 未显示

```powershell
# 检查文件是否存在
Test-Path "$env:USERPROFILE\.claude\skills\gitmoji-commit.md"
Test-Path "$env:USERPROFILE\.claude\skills\grill-me.md"

# 重启 Claude Code
```

### Hook 被绕过

如果团队成员使用 `--no-verify` 绕过：

1. 在 `.gitignore` 中确保已屏蔽敏感文件
2. 在 CI/CD 中添加检查
3. 设置 server-side hooks（如 GitHub Actions）

## 🗑️ 卸载

### 完全卸载

```powershell
# 移除全局 hooks 配置
git config --global --unset core.hooksPath

# 删除 hooks 文件
Remove-Item -Recurse -Force "$env:USERPROFILE\.git-hooks"

# 删除 skills（可选）
Remove-Item -Force "$env:USERPROFILE\.claude\skills\gitmoji-commit.md"
Remove-Item -Force "$env:USERPROFILE\.claude\skills\grill-me.md"
```

### 仅禁用 Hooks（保留文件）

```powershell
git config --global --unset core.hooksPath
```

## 📚 相关资源

- **gitmoji-commit**: https://github.com/final00000000/gitmoji-commit
- **grill-me**: https://github.com/mattpocock/skills
- **Conventional Commits**: https://www.conventionalcommits.org/
- **Gitmoji**: https://gitmoji.dev/

## 🔄 更新

### 更新 Hooks

```powershell
# 重新运行安装脚本
powershell -ExecutionPolicy Bypass .\install-global.ps1
```

### 更新 Skills

```powershell
# 下载最新版本
Invoke-WebRequest -Uri "https://raw.githubusercontent.com/final00000000/gitmoji-commit/main/SKILL.md" -OutFile "$env:USERPROFILE\.claude\skills\gitmoji-commit.md"

Invoke-WebRequest -Uri "https://raw.githubusercontent.com/mattpocock/skills/main/skills/productivity/grill-me/SKILL.md" -OutFile "$env:USERPROFILE\.claude\skills\grill-me.md"
```

## ✅ 验证清单

- [ ] 全局 hooks 路径已配置
- [ ] commit-msg.ps1 文件存在
- [ ] gitmoji-commit skill 已安装
- [ ] grill-me skill 已安装
- [ ] Hooks 在测试仓库中正常工作
- [ ] Skills 在 Claude Code 中可见

## 💬 反馈

遇到问题？检查：

1. PowerShell 版本（推荐 5.1+）
2. Git 版本（推荐 2.9+）
3. Claude Code 版本（推荐最新版）
4. 文件权限和路径
